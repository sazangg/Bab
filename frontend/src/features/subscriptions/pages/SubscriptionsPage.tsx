import { PageHeader } from "@/shared/components/PageHeader";

export function SubscriptionsPage() {
  return (
    <PageHeader
      title="Subscriptions"
      description="Deferred. This area will be rebuilt around allocations and pools."
    />
  );
}
